# Copy this file to backend/.env and change values for your machine.
# Never commit backend/.env because it may contain database passwords.
DATABASE_URL=mysql+pymysql://root:root@localhost/fastapi_crud

# Comma-separated list of browser origins allowed to call this API.
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
